{
  "name": "halcyon",
  "version": "0.3.0",
  "private": true,
  "description": "A plugin runtime for the Discord desktop client with a restrained iOS-style interface.",
  "author": "caitemm (mzrodyu)",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "inject": "node dist/injector.js inject",
    "uninject": "node dist/injector.js uninject"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "@types/react": "^18.3.3",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.3"
  }
}
